# Target to scan (set to your app’s dev/preview URL)
target=https://localhost:8000

# Report format
report_format=html

# Fail threshold (set to 'WARN' or 'FAIL' later if you want blocking)
fail_on_warn=false

# Context file (optional, if you want auth/session)
context_file=
